/*
%d= inteiro;
%.xf = float onde x é o número de casas 
%.c = char;

|--------------------------OBS--------------------------------|
|quando for ler um char, é necessário colocar um espaço       |
| %c no scanf() para evitar que o buffer de entrada seja lido |
|incorretamente.                                              |
|-------------------------------------------------------------|

printf() = saída (terminal)
scanf() = entrada (teclado)
fgets() = leitura de linha (texto com espaço)
*/